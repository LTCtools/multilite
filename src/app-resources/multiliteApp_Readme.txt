The MultiLiteSkeleton.app contains the skeleton to produce the MultiLite Mac application bundle.

To flesh it out it needs:

Everything in the staging directory to be inserted into the:
   MultiLiteSkeleton/Contents/Resources/Java directory

